{
  "name": "minimal-astro-arborium-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d dist -t --nojekyll",
    "postdeploy": "git push",
    "format": "prettier --write ."
  },
  "dependencies": {
    "astro": "^6.0.0-beta.1",
    "hast-util-to-string": "^3.0.1",
    "unist-util-visit": "^5.1.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.8.1"
  }
}
